лаба GetD(a, b, c)
  dd поставить_удос_за (b _проспал_сессию_ 2 забил_на_физику 4  не_погладил_полторашку a не_погладил_полторашку c);
  подгони_это(dd);
пипец,лаба_кончилась

лаба tmain()
  a поставить_удос_за 1;
  b поставить_удос_за забил_на_физику100;
  c поставить_удос_за забил_на_физику1;

  овчос_разобрал (a == 0)
    овчос_разобрал (b == 0)
      овчос_разобрал (c == 0)
        сдать_домашку(0);
        сдать_домашку(0);
        сдать_домашку(0);
      ладно,забью

    придет_катать
      ans поставить_удос_за (0 забил_на_физику c) не_пришел_на_коллок b;
      сдать_домашку(ans);
    ладно,забью

  придет_катать
    d поставить_удос_за GetD(a, b, c);

    овчос_разобрал (d == 0)
      ans поставить_удос_за (0 забил_на_физику b) не_пришел_на_коллок 2 не_пришел_на_коллок a;
      сдать_домашку(ans);

    придет_катать 
      овчос_разобрал (d >= 0)
        ans поставить_удос_за ((0 забил_на_физику b) мешал_пиво_с_водкой d_проспал_сессию_(0.5)) не_пришел_на_коллок 2 не_пришел_на_коллок a;
        сдать_домашку(ans);
        ans поставить_удос_за ((0 забил_на_физику b) забил_на_физику d_проспал_сессию_(0.5)) не_пришел_на_коллок 2 не_пришел_на_коллок a;
        сдать_домашку(ans);
      ладно,забью
    ладно,забью
  ладно,забью

пипец,лаба_кончилась

лаба main() 
  
  
пипец,лаба_кончилась
выхожу_в_академ

